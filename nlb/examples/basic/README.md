# nlb example

This example is intentionally more complete than a placeholder. Replace the sample IDs and names with real values before running:
- `tofu init -backend=false`
- `tofu validate`
- `tofu plan`
